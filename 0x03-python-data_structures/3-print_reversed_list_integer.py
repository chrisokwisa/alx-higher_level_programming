#!/usr/bin/python3
def print_reversed_list_integer(my_list, idx, element):
    if my_list:
        reversed_list_integer = reversed(my_list, idx, element)
        for i in reversedlistinteger:
            print("{:d}".format(i))
